# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| `1.2.x` | 支持 |
| 其他版本 | 暂不承诺提供安全修复 |

## 报告安全漏洞

请勿通过公开 Issue、Pull Request 或讨论区披露未修复的安全漏洞。

如果仓库已启用 GitHub Private Vulnerability Reporting，请在仓库的 **Security and quality → Advisories** 页面使用 **Report a vulnerability** 提交私密报告。该渠道仅对仓库维护者和报告者可见。

如果页面没有 **Report a vulnerability** 入口，请先提交一条不包含漏洞细节的公开 Issue，请维护者提供私下联系渠道。公开 Issue 中不得包含以下内容：

- JM 账号、密码或代理认证信息；
- Cookie、访问令牌、API Key 或其他凭据；
- 包含个人目录、邮箱、用户名或身份信息的完整日志；
- 下载内容、收藏夹导出或成人内容截图。

私密报告建议包含：

- 受影响的版本或提交号；
- 操作系统和运行环境；
- 最小化的复现步骤；
- 预期行为、实际行为及可能影响。

提交前请删除凭据、个人数据和与复现无关的内容。维护者会根据问题的可复现性、影响范围和严重程度进行确认、修复与后续沟通。

## 协调披露

在修复方案可用或双方确认披露时间之前，请不要公开发布漏洞细节、利用代码或受影响用户数据。修复完成后，维护者会在必要范围内更新版本说明或安全公告。

## 本地数据与运行安全

- GUI 设置保存在 `%LOCALAPPDATA%\JMComic-Downloader-GUI\settings.json`，并兼容读取旧版设置。
- 下载内容默认保存在程序所在目录的 `下载` 文件夹。
- 浏览器扩展只读取当前窗口标签页的标题和 URL，用于本地筛选与复制，不会主动上传这些信息。
- 项目不会要求用户关闭杀毒软件。Windows SmartScreen 或杀毒软件可能对未签名的 PyInstaller EXE 给出提示；请从正式 Release 下载并核对 SHA256。

## 使用边界

本项目不托管或分发漫画内容。请仅下载自己有权访问和保存的内容，并遵守所在地法律、网站条款和版权要求。
